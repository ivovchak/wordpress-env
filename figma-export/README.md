# WordPress → Figma: Експорт Компонентів

Інструмент для автоматичної генерації HTML компонентів та Design Tokens з WordPress теми для імпорту в Figma.

## 🎯 Що генерується

### Компоненти
- ✅ **Кнопки** (Primary, Secondary, Outline, різні розміри)
- ✅ **Форми** (Input, Textarea, Select, Checkbox, Radio)
- ✅ **Типографіка** (H1-H6, параграфи)
- ✅ **Cards** (з зображенням і без)
- ✅ **Навігація** (Menu, Breadcrumbs)

### Design Tokens
- 🎨 **Кольори** (Primary, Secondary, Success, Danger, Warning, Info)
- 📝 **Типографіка** (Font families, sizes, weights, line heights)
- 📏 **Spacing** (Margins, paddings)
- 🔲 **Border Radius**
- 🌑 **Shadows**

## 🚀 Швидкий старт

### 1. Генерація компонентів

```bash
cd figma-export
npm run extract
```

Це створить:
```
output/
├── components/
│   ├── buttons.html      # Усі типи кнопок
│   ├── forms.html        # Форми та інпути
│   ├── typography.html   # Заголовки та текст
│   ├── cards.html        # Картки
│   └── navigation.html   # Навігаційні елементи
└── tokens/
    └── design-tokens.json # Design Tokens для Figma
```

### 2. Перегляд компонентів

Відкрийте HTML файли у браузері або запустіть локальний сервер:

```bash
npm run serve
```

Відкриється браузер з переглядом компонентів на `http://localhost:3001`

## 📥 Імпорт в Figma

### Метод 1: HTML → Figma (Рекомендовано)

1. **Встановіть плагін** [html.to.design](https://www.figma.com/community/plugin/1159123024924461424/html-to-design)

2. **Відкрийте HTML файл** (наприклад `buttons.html`) у браузері

3. **Виберіть компонент**:
   - Відкрийте DevTools (F12)
   - Знайдіть потрібний компонент в інспекторі
   - Скопіюйте HTML елемент (Right Click → Copy → Copy element)

4. **У Figma**:
   - Запустіть плагін html.to.design
   - Вставте скопійований HTML
   - Натисніть "Import"

5. **Готово!** Компонент з'явиться в Figma з усіма стилями

### Метод 2: Design Tokens

1. **Встановіть плагін** [Figma Tokens](https://www.figma.com/community/plugin/843461159747178978/Figma-Tokens)

2. **У Figma**:
   - Запустіть плагін Figma Tokens
   - Settings → Import
   - Виберіть файл `output/tokens/design-tokens.json`

3. **Використання**:
   - Токени з'являться в плагіні
   - Застосовуйте їх до елементів через плагін
   - Всі зміни токенів автоматично оновлюються

### Метод 3: Скріншоти (Швидкий спосіб)

1. Відкрийте HTML файли в браузері
2. Зробіть скріншоти окремих компонентів
3. Імпортуйте в Figma як reference images
4. Відтворіть компоненти вручну з точними параметрами

## 🎨 Налаштування кольорів та стилів

### Редагування Design Tokens

Відкрийте `component-extractor.js` і змініть значення в об'єкті `this.tokens`:

```javascript
this.tokens = {
  colors: {
    primary: '#007bff',    // Змініть на ваш колір
    secondary: '#6c757d',
    // ...
  },
  typography: {
    fontFamily: {
      base: 'Your Font Family'
    },
    // ...
  }
}
```

Після змін запустіть знову:
```bash
npm run extract
```

## 📋 Покрокова інструкція імпорту

### Сценарій 1: Імпорт усіх кнопок

1. Відкрийте `output/components/buttons.html` у браузері
2. У Figma створіть нову сторінку "Components"
3. Для кожної групи кнопок:
   - У DevTools виберіть `.component-group`
   - Copy element
   - У Figma: плагін html.to.design → Paste → Import
4. Перетворіть на Figma Components (Ctrl/Cmd + Alt + K)

### Сценарій 2: Створення Design System

1. **Імпортуйте tokens**:
   - Figma Tokens плагін → Import `design-tokens.json`

2. **Створіть Color Styles**:
   - У Figma виберіть елемент
   - Local styles → + Create style
   - Назвіть відповідно до токенів (primary, secondary...)

3. **Створіть Text Styles**:
   - Для кожного заголовка H1-H6
   - Create text style
   - Застосуйте параметри з токенів

4. **Імпортуйте компоненти**:
   - Використайте html.to.design для кожного HTML файлу
   - Застосуйте створені styles

## 🔧 Додаткові можливості

### Додавання власних компонентів

У `component-extractor.js` додайте новий метод:

```javascript
generateMyComponent() {
  return `
    <div class="component-group">
      <h2 class="component-title">Мій Компонент</h2>
      <div class="component-items">
        <!-- Ваш HTML -->
      </div>
    </div>
  `;
}
```

І додайте до `generateComponents()`:

```javascript
const components = {
  buttons: this.generateButtons(),
  myComponent: this.generateMyComponent(), // ← додати тут
  // ...
};
```

### Експорт з реального WordPress сайту

Якщо у вас вже працює WordPress:

1. Відкрийте сайт у браузері
2. У DevTools виберіть потрібний елемент
3. Copy → Copy element
4. Створіть новий HTML файл в `output/components/`
5. Вставте скопійований код
6. Імпортуйте через html.to.design

## 💡 Поради

### Оптимізація робочого процесу

1. **Групуйте компоненти**: Імпортуйте по категоріям (усі кнопки, потім усі форми)
2. **Створюйте Auto Layout**: Після імпорту застосуйте Auto Layout до контейнерів
3. **Використовуйте Variants**: Об'єднайте схожі компоненти у Figma Variants
4. **Зберігайте ієрархію**: Використовуйте frames для організації компонентів

### Типові проблеми

**Проблема**: Стилі не імпортуються коректно
- **Рішення**: Переконайтесь, що використовуєте inline styles (вже реалізовано в скрипті)

**Проблема**: Шрифти відображаються неправильно
- **Рішення**: Встановіть потрібні шрифти в систему або змініть `fontFamily` в токенах

**Проблема**: Розміри компонентів не відповідають
- **Рішення**: Перевірте viewport та масштаб при копіюванні HTML

## 📚 Корисні ресурси

- [html.to.design плагін](https://www.figma.com/community/plugin/1159123024924461424)
- [Figma Tokens плагін](https://www.figma.com/community/plugin/843461159747178978)
- [Bootstrap документація](https://getbootstrap.com/docs/)
- [Understrap тема](https://understrap.com/)

## 🎯 Наступні кроки

Після імпорту компонентів:

1. ✅ Створіть Component Library в Figma
2. ✅ Налаштуйте Auto Layout для responsive компонентів
3. ✅ Додайте Variants для різних станів (hover, active, disabled)
4. ✅ Створіть Documentation для команди
5. ✅ Опублікуйте в Team Library для повторного використання

---

**Автор**: WordPress to Figma Exporter
**Версія**: 1.0.0
**Ліцензія**: MIT
