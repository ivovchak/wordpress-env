# 🚀 Швидкий старт

## Крок 1: Генерація компонентів

```bash
cd figma-export
node component-extractor.js
```

## Крок 2: Перегляд компонентів

Відкрийте у браузері:
```
file:///home/user/wordpress-env/figma-export/output/index.html
```

Або через HTTP сервер:
```bash
npm run serve
```

## Крок 3: Імпорт в Figma

### Варіант A: HTML → Figma (найпростіший)

1. **Встановіть плагін** [html.to.design](https://www.figma.com/community/plugin/1159123024924461424)

2. **Відкрийте** один з HTML файлів, наприклад `buttons.html`

3. **У браузері**:
   - Натисніть F12 (DevTools)
   - Знайдіть потрібний компонент (кнопку, форму тощо)
   - Right Click → Copy → Copy element

4. **У Figma**:
   - Plugins → html.to.design
   - Paste HTML code
   - Click "Import to Figma"
   - Готово! 🎉

### Варіант B: Design Tokens

1. **Встановіть плагін** [Figma Tokens](https://www.figma.com/community/plugin/843461159747178978)

2. **У Figma**:
   - Plugins → Figma Tokens
   - Settings → Import
   - Виберіть `output/tokens/design-tokens.json`

3. Використовуйте токени для створення стилів

## Додавання власних компонентів

### 1. Редагуйте `component-extractor.js`

Додайте новий метод для вашого компонента:

```javascript
generateMyCustomComponent() {
  return `
    <div class="component-group">
      <h2 class="component-title">Мій Компонент</h2>
      <div class="component-items">
        <div style="
          padding: 1rem;
          background: ${this.tokens.colors.primary};
          color: white;
          border-radius: ${this.tokens.borderRadius.base};
        ">
          Привіт, це мій компонент!
        </div>
      </div>
    </div>
  `;
}
```

### 2. Додайте до списку компонентів

У методі `generateComponents()`:

```javascript
const components = {
  buttons: this.generateButtons(),
  forms: this.generateForms(),
  myCustom: this.generateMyCustomComponent(), // ← тут
  // ...
};
```

### 3. Регенеруйте

```bash
node component-extractor.js
```

## Налаштування кольорів

Змініть кольори в `component-extractor.js`:

```javascript
this.tokens = {
  colors: {
    primary: '#ВАШ_КОЛІР',    // ← змініть тут
    secondary: '#ВАШ_КОЛІР',
    // ...
  }
}
```

## Експорт з реального WordPress

Якщо у вас вже працює WordPress:

1. **Запустіть WordPress**:
```bash
docker compose up
```

2. **Відкрийте сайт** у браузері: `http://localhost:8000`

3. **Скопіюйте HTML**:
   - F12 → Elements
   - Знайдіть потрібний елемент
   - Copy element

4. **Створіть новий HTML файл** в `output/components/`:
```bash
touch output/components/my-real-component.html
```

5. **Вставте скопійований HTML** та додайте wrapper з `index.html`

6. **Імпортуйте в Figma** через html.to.design

## Поради

### ✅ Що працює добре:
- Прості компоненти (кнопки, інпути)
- Inline стилі (вже реалізовано)
- Flexbox та Grid layouts
- Кольори, шрифти, відступи

### ⚠️ Що може потребувати доопрацювання:
- Складні анімації
- JavaScript інтерактивність
- Абсолютне позиціонування
- Background images (використовуйте `<img>` замість CSS background)

## Troubleshooting

**Проблема**: Шрифти не відображаються
- **Рішення**: Встановіть шрифт в систему або використовуйте Google Fonts

**Проблема**: Кольори відрізняються
- **Рішення**: Перевірте `design-tokens.json` та порівняйте з WordPress

**Проблема**: Layout ламається
- **Рішення**: Використовуйте Figma Auto Layout після імпорту

## Корисні команди

```bash
# Регенерація всіх компонентів
node component-extractor.js

# Локальний сервер для перегляду
npm run serve

# Очистити output
rm -rf output && node component-extractor.js
```

## Наступні кроки

1. ✅ Імпортуйте компоненти в Figma
2. ✅ Створіть Figma Components з імпортованих елементів
3. ✅ Додайте Variants для різних станів (hover, active)
4. ✅ Налаштуйте Auto Layout
5. ✅ Опублікуйте в Team Library
6. ✅ Поділіться з командою!

---

**Питання?** Читайте повну документацію в `README.md`
